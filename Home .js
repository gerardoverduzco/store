import { NavigationContainer } from '@react-navigation/native';
import React from 'react';
import { Button } from 'react-native-web';

function Home() {
    return (
 
      <Home>
        <Button name="Home" component={Home} />
        <Button name="Atras" component={Atras} />
      </Home>
      
    );
  }
   
   

 
export default Home ;